\begin{problem}{\(P|\cdot|C_{\max}\)}
    \Input{\(n\) taches de durées \(p_1,\ldots,p_n\) indépendantes}
    \Question{Ordonnancer ces tâches sur \(m\) machines identiques en une durée totale minimale notée \(C_{\max}\).}
\end{problem}
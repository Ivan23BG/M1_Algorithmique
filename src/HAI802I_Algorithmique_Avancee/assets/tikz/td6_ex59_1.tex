\begin{problem}{\(2\)-\textsc{Partition}}
    \Input{Un ensemble de \(n\) objets \(a_i\) de poids \(s(a_i)\) de somme \(2p\)}
    \Question{Est-il possible de trouver une partition en deux sous-ensemble de poids total \(p\)}
\end{problem}
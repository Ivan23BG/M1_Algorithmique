\begin{algorithm}[H]
    \caption{\textsc{Vertex Cover}}
    \begin{algorithmic}
        \Require{Un graphe \(G = (V, E)\), un entier \(k\)}
        \Ensure{Trouver \(V' \sub V\) telle que chaque arête est couverte par un sommet de \(V\) avec \(|V'| = k\)}
    \end{algorithmic}
\end{algorithm}
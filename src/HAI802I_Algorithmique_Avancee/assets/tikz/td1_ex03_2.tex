\begin{problem}{\textsc{Vertex Cover}}
    \Input{Un graphe \(G = (V, E)\), un entier \(k\)}
    \Question{Trouver \(V' \sub V\) telle que chaque arête est couverte par un sommet de \(V\) avec \(|V'| = k\)}
\end{problem}
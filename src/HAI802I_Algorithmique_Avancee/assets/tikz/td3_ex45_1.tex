\begin{problem}{\textsc{Asymmetric TSP Problem (ATSP)}}
    \Input{Un graphe \(G = (V, E)\) orienté et muni d'une fonction de coût positive sur les arcs.}
    \Question{Trouver un circuit de cout minimum qui passe par chaque sommet exactement une fois.}
\end{problem}